                                     <a class="btn btn-primary" href="{{ path(pathEdit, paramEdit)}}">
                                        <i class="fa fa-pencil-square" aria-hidden="true"></i>
                                    </a>



                                    <a class="btn btn-danger" href="#" data-toggle="modal" data-target="#exampleModal">
                                        <i class="{{btnDelete}}" aria-hidden="true"></i>
                                    </a>

                                    <!-- Modal -->
                                    <div class="modal fade border-danger" id="exampleModal" tabindex="-1" role="dialog" aria-labelledby="exampleModalLabel" aria-hidden="true">
                                        <div class="modal-dialog" role="document">
                                            <div class="modal-content">
                                                <div class="modal-header">

                                                    {% if btnDelete == 'fa fa-trash' %}

                                                        <h5 class="modal-title text-success" id="exampleModalLabel">{{Text_Delete}}</h5>
                        
                                                    {% endif %}

                                                    <button type="button" class="close" data-dismiss="modal" aria-label="Close">
                                                    <span aria-hidden="true">&times;</span>
                                                    </button>
                                                </div>
                                                <div class="modal-body">

                                                    {% if btnDelete == 'fa fa-trash' %}
                                                        {{ SBody}}
                                                    {% endif %}
                                                    
                                                </div>
                                                <div class="modal-footer">

                                                    {% if btnDelete == 'fa fa-trash' %}
                                                        <a class="btn btn-danger" href="{{ path(path, param) }}">
                                                             Oui
                                                        </a>

                                                        <a href="{{ path(paths)}}">
                                                            <button type="button" class="btn btn-secondary" data-dismiss="modal">Non</button>
                                                        </a>
                                                         
                                                    {% endif %}

                                                    

                                                
                                                </div>
                                            </div>
                                        </div>
                                    </div>