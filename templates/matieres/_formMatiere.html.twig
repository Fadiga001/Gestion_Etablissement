<div class="container w-50">
                    {{ form_start(form) }}

                            <div class="form-row">
                                <div class="form-group">
                                    
                                    {{ form_row(form.codeMatiere) }}
                                </div>
                            </div>
                            

                            <div class="form-group">
                                {{ form_row(form.denomination) }}
                            </div>

                            <div class="form-group">
                                {{ form_row(form.TypeMatiere) }}
                            </div>

                            <div class="form-group">
                                {{ form_row(form.prof) }}
                            </div>

                            <div class="form-group">
                                {{ form_row(form.classe) }}
                            </div>
                          
                            <div class="form-submit">

                                {% include 'configuration/btnTest.html.twig' %}
                                
                            </div>
                        
                        {{ form_end(form) }}
                </div>