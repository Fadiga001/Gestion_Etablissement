<div class="container w-80 d-flex justify-content-center">
                    {{ form_start(form) }}

                            <div class="form-row">
                                <div class="form-group mr-3">
                                    
                                    {{ form_row(form.codeMatiere) }}
                                </div>

                                <div class="form-group mr-3">
                                    {{ form_row(form.denomination) }}
                                </div>

                                <div class="form-group mr-3">
                                    {{ form_row(form.coefficient) }}
                                </div>
                            </div>

                            <div class="form-row">
                                <div class="form-group mr-3">
                                    {{ form_row(form.TypeMatiere) }}
                                </div>

                                <div class="form-group mr-3">
                                    {{ form_row(form.prof) }}
                                </div>
                            </div>

                            <div class="form-group">
                                {{ form_row(form.classe) }}
                            </div>
                          
                            <div class="form-submit">

                                {% include 'configuration/btnTest.html.twig' %}
                                
                            </div>
                        
                        {{ form_end(form) }}
                </div>